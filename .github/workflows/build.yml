name: Build Windows Executable

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller customtkinter cryptography

      # --- بناء البرنامج مع تضمين الأيقونة ---
      - name: Build executable with PyInstaller
        run: pyinstaller --name "FileFortress" --onefile --windowed --icon=icon.ico GodModeEncryptor.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FileFortress-GodMode
          path: dist/FileFortress.exe
